const titulo = 'Isto é um titulo longo de um livro';
console.log(titulo.length);


const trocarPlavara = titulo.replace('livro', 'diário');

console.log(trocarPlavara);

const novaFrase = titulo + ' mas não faz mal porque não é uma seca';

console.log(novaFrase);

const quatroCaracteres = titulo.slice(0, 4);

console.log(quatroCaracteres)